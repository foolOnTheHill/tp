#ifndef WU_MANBER_H_
#define WU_MANBER_H_

#include <stdio.h>
#include <string>
#include <vector>

#define ALPHABET_LENGTH 256

using namespace std;

vector<int> wu_manber(string text, string pattern, int r);

#endif
